{
  "package-name": "native_active_window",
  "version": "1.0.0",
  "description": "native-active-window",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-gyp": "^10.0.1"
  },
  "dependencies": {
    "node-addon-api": "^7.1.0"
  },
  "gypfile": true
}
